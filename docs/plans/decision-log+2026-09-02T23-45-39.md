# Conversation Log: What We're Working On — Kamila Bot

- **Plan/reference name:** `decision-log`
- **Created:** 2026-09-02T23:45:39+03:00 (ID suffix: `2026-09-02T23-45-39`)
- **Purpose:** Two-way traffic record of the decisions, questions, and facts exchanged
  in this session so you know **what we're working on and why**.

---

## 1. Session threads (all active)

| # | Thread | Status | Where documented |
|---|---|---|---|
| 1 | npm vulnerabilities | ✅ `qs` fixed; ⚠️ `extract-zip` accepted risk | `security-and-refactor+2026-09-02T23-45-39.md` §2 |
| 2 | Phase 4 code review findings | ✅ planned | `security-and-refactor+2026-09-02T23-45-39.md` §3-4 |
| 3 | WhatsApp login failure (Chrome) | 🔧 diagnosed, fix command given | `security-and-refactor+2026-09-02T23-45-39.md` §2 |
| 4 | Dashboard UI/UX redesign | ✅ planned | `dashboard-ui-ux+2026-09-02T23-45-39.md` |
| 5 | Storage question (SQLite vs JSON) | ℹ️ answered, **no change** requested | §5 below |
| 6 | Dashboard improvements (Settings fix, loopback auth, UX polish) | 📋 documented | `dashboard-improvements+2026-09-03T00-14-17.md` + §8 below |

---

## 2. npm vulnerability work

**Question asked:** "how can you fix the vulnerabilities of npm"

**Findings & resolution:**
- `npm audit` showed 8 vulns (1 low, 1 moderate, 6 high) — nearly all traceable to
  `whatsapp-web.js` → `puppeteer@18.x` → `extract-zip`, `tar-fs`, `ws`, plus `body-parser`/`qs`.
- Ran `npm audit fix`:
  - `whatsapp-web.js` → 1.34.7, `puppeteer` → 24.38.0, `tar-fs` → 3.1.3, `ws` → 8.21.3, `body-parser` → 1.20.6.
- Added `package.json` override `"qs": "^6.16.0"` to clear the 2 moderate `qs` DoS vulns.
- **Remaining (5 HIGH, `extract-zip`):** no safe fix — `@puppeteer/browsers` 3.x is
  Node≥22 + API-incompatible with the pinned `puppeteer-core@24.38.0`. Accepted risk,
  documented. `extract-zip` only runs during browser install, not message handling.

**User note (important):** *"btw ths is locally runned like i do it only in my pc"*
→ Local, single-PC deployment. Lowers severity of network-exposure issues, but
security hygiene still applied.

---

## 3. Phase 4 code review — all findings

**Question asked (implied):** assess how strong the code is.

Two independent `code-reviewer` runs returned consistent findings. Summary:

| Severity | Count | Key items |
|---|---|---|
| CRITICAL | 5 | Stored XSS (`innerHTML`), no auth on API, Tailwind CDN supply-chain, prompt injection, arbitrary message send |
| HIGH | 6 | Map memory leaks, no CSP/headers, listens on 0.0.0.0, dead `bot.js`, unsafe `JSON.parse` on LLM out, fire-and-forget eval |
| MEDIUM | 9 | no input validation, magic numbers, console noise, no `.gitignore`, no tests, inline HTML blob, body-parser redundancy, insecure puppeteer defaults |
| LOW | 3 | module inconsistency, missing JSDoc, qs override note |

**Verdict:** ❌ BLOCK until CRITICAL/HIGH addressed. See
`docs/reviews/2026-09-02T23-21-39/` for full line-referenced reports.

---

## 4. WhatsApp login failure — Chrome version mismatch

**User report:** "i tried it and sayed it cant conect to it" (login failed).

**Diagnosis (read-only):**
- `whatsapp-web.js@1.34.7` → `puppeteer@24.38.0` pins Chrome **`146.0.7680.31`**
  (`node_modules/puppeteer-core/lib/cjs/puppeteer/revisions.js`).
- The cache `/home/blue/.cache/puppeteer/chrome/` had been changed by
  `rm -rf .../146.0.7680.31 ...` + `npx puppeteer browsers install chrome@stable`,
  resulting in **v152.0.7977.75** — the **wrong revision**.
- Puppeteer throws `Could not find Chrome (ver. 146.0.7680.31)` → bot can't connect.

**Fix (command given, not yet confirmed run):**
```bash
npx puppeteer browsers install chrome@146.0.7680.31
```
(Or delete the v152 cache; the bot then auto-downloads the pinned 146 build.)

---

## 5. Storage question — do we need SQLite?

**Question asked:** "is there need for sqlite cant we use json or sth"

**Answer (no change requested):** Not strictly — the data is small and all queries are
simple keyed lookups (no joins/aggregation). A JSON store or `sql.js` (WASM) would work
and eliminate the native-build failure. But the user was "just asking," so **no change
was made**; `better-sqlite3` stayed. (Note: `better-sqlite3@9.6.0` failed to build on
Node 26 per `logs/sqlite-output.log`; upgrading is an option if it becomes a blocker.)

---

## 6. Dashboard UI/UX — decisions made

**Question asked:** work out a plan for the dashboard UI/UX, make it more like a useful
dashboard.

**Decisions (user-confirmed):**
| Question | Decision |
|---|---|
| Overall structure | **Multi-page SPA-style** (hash router) |
| Charts/visuals | **Chart.js via CDN** (jsdelivr, pinned + SRI) |
| Real-time | **SSE (Server-Sent Events)** |
| Structure/headers (earlier) | Multi-page SPA |
| Security headers (earlier) | **helmet + real CSP** — since deferred (see §8 update) |
| API auth (earlier) | **loopback + origin + shared token** — ⚠️ updated 2026-09-03 → **loopback-only** (see §8) |
| Test framework (earlier) | **node:test** |

Full detail in `dashboard-ui-ux+2026-09-02T23-45-39.md`.

---

## 8. UPDATE 2026-09-03T00:14 — Dashboard improvement decisions (roll-forward)

The original SPA plan is now **implemented**. Three follow-up workstreams were scoped and
documented in a new plan: `dashboard-improvements+2026-09-03T00-14-17.md`.

**New user-confirmed decisions (supersede earlier entries):**
| Question | Result |
|---|---|
| Auth strength | **Loopback-only bind** (`127.0.0.1`). **Supersedes** the earlier "loopback + origin + shared token" choice. No token, no origin allowlist, no client changes. |
| Typing indicator | **Yes** — add an SSE `typing` event + "Kamila is typing…" bubble. |

**Clarification note:** an earlier laconic answer ("just once") did not map to the offered auth
options; re-asking resolved it to **Loopback-only (simplest)**. This reverses the API-auth row in
§6. helmet/CSP is **deferred** (not part of the loopback workstream).

**Workstreams (full detail in the new plan):**
1. Loopback-only bind.
2. Fix Settings view (add `GET /api/config`; replace hardcoded `'kamila'`/`30000` values).
3. UX polish: typing indicator, diff-aware contact list, draft expiry, needs-help badges,
   loading skeletons, accessibility.

**Not in scope (documented, not selected):** redundant `setInterval` polling cleanup,
helmet/CSP, Chart.js SRI hardening.

---

## 7. Next actions (once you confirm)

1. Confirm the 3 dashboard open items (Chart.js host/SRI, draft persistence to a table,
   SSE auth mechanism) in `dashboard-ui-ux+2026-09-02T23-45-39.md` §9.
2. Confirm execution of these plans (currently documented, not yet implemented).
3. Consider confirming the Chrome `@146.0.7680.31` reinstall to restore WhatsApp login.

---
*End of decision log.*
